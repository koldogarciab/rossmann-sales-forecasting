# Raw data

Place the original Kaggle files here using these exact names:

- `train.csv`
- `store.csv`
- `test.csv`
- `sample_submission.csv`

Do not commit these files to Git. This folder is excluded through `.gitignore`.
